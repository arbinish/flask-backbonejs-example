flask-backbonejs-example
========================

App using flask as backend and backbonejs as frontend

Usage:
pip install -r requirements.txt

User database in users.db

Run:
python ./rest.py

Open http://localhost:5000/ in a browser
